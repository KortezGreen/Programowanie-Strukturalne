#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, bez;

    scanf("%d", &n);

    if(n<0)
    {
        bez=n*(-1);
    }
    else
    {
        bez=n;
    }

    printf("%d\n", bez);

    return 0;
}
