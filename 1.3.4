#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, naj;
    scanf("%d %d", &a, &b);

    if(abs(a)<abs(b))
    {
        naj=b;
    }
    if(abs(a)<abs(b))
    {
        naj=a;
    }
    else
    {
        naj=a;
    }

    printf("%d", naj);

    return 0;
}
